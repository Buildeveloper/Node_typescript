// Arrow function são sempre funções anonimas

// Retorno explicito
const soma = (num1,num2) => {
    return num1 + num2;
}

// Retorno implicito
const soma2 = (num1,num2) => num1 + num2;

// Caso tenha apenas um argumento
// os parenteses não são aobrigatórios
const soma3 = num => num + 3;

