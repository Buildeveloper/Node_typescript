// Definindo tipos primitivos para variaveis 
let nVariavel: number = 10;
let bVariavel: boolean = true;

// tipos por inferencia
let aVariavel: any = 'qualquercoisa';
aVariavel = 10; // etc tec


// functions
function soma(a: number, b: number): number {
    return a + b;
}